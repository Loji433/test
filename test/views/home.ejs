<h3>You are logged in <%= currentUser.firstname %>!</h3>
<div>
    <% if(currentUser.isAdmin) { %>
        <% users.forEach( user => { %>
            <div><%= user.firstname %></div>
        <% }) %>
    <% } %>
</div>